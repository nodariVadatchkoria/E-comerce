import { AuthPermissionsDirective } from './auth-permissions.directive';

describe('AuthPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
